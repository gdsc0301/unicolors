const path = require('path');

module.exports = {
    entry: './src/index.ts',
    module: {
        rules: [
            {
                test: /index.ts$/,
                loader: 'babel-loader',
            }
        ],
    },
    mode: 'development',
    resolve: {
        extensions: ['.ts'],
    },
    output: {
        filename: 'unicolors.js',
        path: path.resolve(__dirname, 'dist'),
        library: {
            type: 'umd'
        }
    },
};